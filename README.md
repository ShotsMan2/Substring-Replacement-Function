# Substring-Replacement-Function
A C function that searches for a specific substring within a source text and replaces all occurrences with a new string. It constructs the new text manually without using the 'strstr' library function.
